# Needed only when the Helm OCI package is private.
# Store the token through Sealed Secrets, External Secrets, or another secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: ghcr-helm-oci
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  name: ghcr-jenkins-mcp
  type: helm
  enableOCI: "true"
  url: ghcr.io/grglzrv/charts
  username: grglzrv
  password: REPLACE_WITH_PAT_CLASSIC_READ_PACKAGES
