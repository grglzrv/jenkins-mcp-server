# Original source

This project originated from and preserves attribution to:

- Repository: `https://github.com/akhilthomas236/jenkins_mcp_server`
- PyPI package: `jenkins-mcp-server` version `0.1.6`
- License: MIT

The implementation in this archive is a substantial production-focused rewrite rather than an unchanged mirror. Git history is not included because the build environment could not connect to GitHub through Git; the original repository and license attribution are retained here and in `LICENSE` and `README.md`.
